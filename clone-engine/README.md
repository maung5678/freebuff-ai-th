# Clone engine

During local development, copy the tested `Manage-Freebuff-Clones.ps1` from the Clone Freebuff project into this folder before packaging. The manager invokes it only for explicit rebuild operations.
