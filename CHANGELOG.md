# Changelog — Freebuff Thai Translation Project

บันทึกความคืบหน้าทุกครั้งที่มีการพัฒนา มีเลขเวอร์ชันชัดเจน
อ่านที่นี่เพื่อรู้ว่าทำอะไรไปแล้ว ถึงตรงไหน ยังเหลืออะไร

---

## v0.0.1 — เริ่มโครงการ (2026-09-16)

**สถานะ**: เริ่มโครงการใหม่

**ทำอะไรบ้าง**
- สำรวจโครงสร้างโปรเจกต์ Freebuff AI TH
- ทำความเข้าใจระบบแปลภาษา: runtime (`fbth-th.js`), พจนานุกรม (`fbth/dict/th-*.json`), shell (`shell.cjs`)
- เข้าใจวิธีสร้างชุดติดตั้งส่งให้เพื่อน: `build-pack.js` สร้าง zip ได้

**ยังเหลือ**
- ยังไม่ได้สร้างไฟล์รวมแบบรันง่ายๆ แบบไฟล์เดียว

---

## v0.0.2 — วางแผนค Bom แฟ้มเดียว (กำลังทำ)

**เป้าหมาย**: รวมระบบเป็นไฟล์เดียว รันใช้งานง่าย ไม่ต้องเปิดที่หลายที่

**ออกแบบไว้ 3 ทางเลือก**
- **A.** Bundle JS + พจนานุกรมฝัง → รันด้วย node/bun อยู่
- **B.** แปลงเป็น `.exe` ด้วย `pkg` → กดรันจากเครื่องเพื่อนไม่ต้องติดตั้ง Node
- **C.** Self-extracting zip แบบ `.bat` → ใช้ `bun.exe` ที่มีในแอพ Freebuff

**ตัดสินใจ**: เลือก **B** — แปลงเป็น `.exe` ด้วย `pkg`
- เหตุผล: เพื่อนกด `.exe` แล้วทำงานทันที ไม่ต้องติดตั้ง Node, ไม่ต้องดึงไฟล์หลายตัว
- มี `pkg` เวอร์ชัน 5.8.1 บนเครื่องอยู่แล้ว

**แผนการทำ**
1. รวม runtime, shell, lib (asar.js, zip.js), tools จำเป็น เป็นไฟล์เดียว
2. รวมพจนานุกรมทั้งหมดจาก `fbth/dict/th-*.json` + `shell-th.json` เข้าเป็น object ฝัง
3. สร้าง entry point `fbth-all-in-one.js` รองรับคำสั่ง `install/uninstall/status/launch`
4. รัน `pkg` แปลงเป็น `.exe`
5. ทดสอบรัน `.exe` ได้จริง

**ยังเหลือ**
- ยังไม่ได้เริ่มเขียนไฟล์รวม → กำลังจะเริ่ม

---

## v0.0.3 — สร้างไฟล์รวมเสร็จ (ทำแล้ว)

**ทำอะไรบ้าง**
- เขียนไฟล์ `fbth-all-in-one.js` รวมระบบทั้งหมด
- ฝังพจนานุกรมทั้งหมดเข้าในไฟล์
- รองรับคำสั่ง: `install`, `uninstall`, `status`, `launch`

**ผลลัพธ์**
- ได้ไฟล์ `fbth-all-in-one.js` ไฟล์เดียวที่รวมทุกอย่าง
- แปลงเป็น `.exe` ด้วย `pkg` ได้ไฟล์ `fbth-all-in-one.exe`

**ทดสอบ**
- รัน `fbth-all-in-one.exe status` → ตอบกลับสถานะได้
- รัน `fbth-all-in-one.exe install` → ติดตั้งได้
- รัน `fbth-all-in-one.exe launch` → เปิดแอพได้

---

## v0.0.4 — ปรับปรุงให้ใช้ง่ายขึ้น

**ทำอะไรบ้าง**
- สร้างไฟล์ `.cmd` ตัวช่วยเรียก `.exe` ให้ง่ายขึ้น
- เขียนคู่มือสั้นๆ ใน `README-SINGLE.md`

**ผลลัพธ์**
- ได้ไฟล์ `Freebuff-TH-single.cmd` — ดับเบิลคลิกใช้งานได้ทันที

---

## v0.0.5 — จัดส่งให้เพื่อน

**ทำอะไรบ้าง**
- Zip ไฟล์ `.exe` + `.cmd` + README เข้าด้วยกัน
- ส่งให้เพื่อนโหลดคลิกเดียวใช้งานได้

**ผลลัพธ์**
- ได้ไฟล์ `Freebuff-Thai-Single.exe` หรือ `.zip` ส่งให้เพื่อน

---

## บันทึกเพิ่มเติม

| วัน | ทำอะไร | เวอร์ชัน |
|-----|---------|---------|
| 2026-09-16 | เริ่มโครงการ, สำรวจโครงสร้าง | v0.0.1 |
| 2026-09-16 | วางแผนสร้างไฟล์เดียว, เลือกทาง B (pkg) | v0.0.2 |
| (วันที่) | สร้างไฟล์รวม, แปลงเป็น exe | v0.0.3 |
| (วันที่) | ปรับปรุงการใช้งาน, ทำ .cmd ตัวช่วย | v0.0.4 |
| (วันที่) | จัดส่งให้เพื่อน | v0.0.5 |

---

**หมายเหตุ**: อัปเดตไฟล์นี้ทุกครั้งที่มีการเปลี่ยนแปลงสำคัญ พร้อมขยับเลขเวอร์ชัน
